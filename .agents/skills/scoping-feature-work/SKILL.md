All 7 files created. Here's what was generated at `.Codex/skills/scoping-feature-work/`:

**SKILL.md** — Quick start with layer-mapping pattern, slice checklist template, MVP boundary decision tree, and links to all references.

**references/**:
- **activation-onboarding.md** — Detection state machine (NOT_KEYSCRIPT → AUTHENTICATED), settings-as-onboarding patterns, empty state requirements for all 3 tool windows, and anti-patterns for empty `keystoneServer` and missing project conditions
- **engagement-adoption.md** — The 7-step engagement funnel (open → run → iterate), surface adoption table, discoverability requirements for hidden features, CORE/POWER/ADMIN categorization, and a scoping checklist for new tool window tabs
- **in-app-guidance.md** — All 5 guidance surfaces with their APIs, status bar copy constraints (≤20 chars), notification balloon patterns, inline panel empty states, disabled action tooltip patterns
- **product-analytics.md** — WARNING about v2.0.0 having zero instrumentation, event prioritization (HIGH/MEDIUM/LOW), natural hook points in `RunKeyscriptService`/`DeploymentService`/`SessionService`, analytics slice template, PII safety rules
- **roadmap-experiments.md** — Semantic versioning in two files, opt-in `KeyscryptSettings` boolean pattern for feature flags, rollout strategy table, experiment scoping template, settings schema migration warning
- **feedback-insights.md** — Feedback sources (Diagnostics panel is the main one), bug-to-scope translation workflow, signal/noise filter, feedback-driven scoping checklist