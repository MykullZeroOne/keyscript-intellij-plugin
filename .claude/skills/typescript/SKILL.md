Done. Here's what was written/verified:

**`SKILL.md`** — rewritten with proper skill frontmatter, a canonical user script example, key files table, concept rules table, workflow checklist for adding type completions, and cross-references to **jcef**, **ktor**, **kotlin**, **gradle**, and **intellij-platform** skills.

**`references/`** — all four files were already complete and accurate:
- `patterns.md` — CR framework patterns, `CR.Core.defer()` usage, `errorCode` check, live template format, esbuild IIFE requirement
- `types.md` — `cr-framework.d.ts` structure, namespace conventions, declaration merging, `any` anti-pattern
- `modules.md` — vendored bundle architecture, global namespace tree, HTML templates as runtime modules, adding new JS modules via Ktor static routes
- `errors.md` — runtime errors (`CR is not defined`, empty records), proxy/network errors, type definition sync checklist, esbuild bundle errors, debugging checklist